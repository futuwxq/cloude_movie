<!-- components/movie-item/index.wxml -->
<view bind:tap="onMovieDetail" class="movie-container">
  <image src="{{movieitem.cover}}" mode="widthFix" />
  <view class="desc">
    <text class="title">{{movieitem.title}}</text>
    <text class="rate">{{movieitem.rate}}</text>
  </view>
</view>